# Binários FFmpeg - não versionar
Coloque os binários aqui para desenvolvimento local.
## Windows:
- ffmpeg.exe, ffprobe.exe (para build Windows via GH Actions)
## macOS/Linux:
-ffmpeg-aarch64-apple-darwin, ffprobe-aarch64-apple-darwin (para dev local no Mac)
